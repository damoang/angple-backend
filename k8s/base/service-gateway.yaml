apiVersion: v1
kind: Service
metadata:
  name: angple-gateway
  namespace: angple
  labels:
    app.kubernetes.io/name: angple-gateway
    app.kubernetes.io/part-of: angple
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: angple-gateway
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
