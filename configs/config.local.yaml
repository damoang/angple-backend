# Local Development Configuration
# APP_ENV=local (기본값)

server:
  port: 8081
  mode: development
  env: local

database:
  host: 127.0.0.1
  port: 3307
  user: angple_user
  password: angple_pass_2024
  dbname: angple_db
  max_idle_conns: 10
  max_open_conns: 100
  conn_max_lifetime: 3600

redis:
  host: localhost
  port: 6381
  password: ""
  db: 0
  pool_size: 100

jwt:
  secret: local-dev-secret-key
  expires_in: 900
  refresh_in: 604800
  damoang_secret: ".damoang.net-jwt-secret"

data_paths:
  recommended_path: "./data/recommended"  # 로컬 개발용 경로

cors:
  allow_origins: "http://localhost:5173, http://localhost:5174, http://localhost:5175, http://localhost:3000"
